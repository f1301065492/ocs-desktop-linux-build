@.claude/skills/project-context.md

## 工作约定（长期生效，用户 2026-09 定）

- **每次修改后更新 `交接文件.md`（仓库根目录）。** 它是本机的工作记录：本轮做了什么、
  根因是什么、验证到什么程度、遗留什么。改动落地、构建有结果时就顺手补上，
  不要攒到最后一次性补。
  ⚠️ 该文件（以及 `OCS网关待补接口.md`）**只留本地：不要提交、不要推送**，
  两者都已写进 `.gitignore`（所以它们在仓库里不存在，上面不加链接）。提交时按路径显式
  `git add`，别用 `git add -A`。
- **发版只顺延版本号第三位**（patch）。`major.minor` 只有在用户明确要求时才动。
  `.github/workflows/release.yml` 里已删掉「按提交内容自动判断」（有 `feat` 就升 minor）的逻辑，
  默认与唯一常用值都是 `patch`；传 `bump=auto` 会被 422 拒绝。
